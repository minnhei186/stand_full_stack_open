{
  "name": "backend",
  "version": "0.0.1",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js",
    "build:ui": "rm -rf dist && cd ../frontend && npm ci && npm run build && cp -r dist ../buckend",
    "deploy": "gcloud run deploy my-service --source . --region=asia-northeast1 --allow-unauthenticated",
    "deploy:full": "npm run build:ui && npm run deploy",
    "test": "echo \\\"Error: no test specified\\\" && exit 1\""
  },
  "author": "Matti Luukkainen",
  "license": "MIT",
  "dependencies": {
    "express": "^4.21.2"
  }
}
